Day07 demos from Day07_Demo_Guide.md

Run demos with the virtualenv at `temp/.venv`.

Setup:

    cd temp
    python3 -m venv .venv
    ./.venv/bin/pip install -r day07/requirements.txt

Run tests:

    ./.venv/bin/python -m pytest -q day07
